<%- include('../partials/admin-header') %>
<% if (success) { %><div class="alert alert-success"><span class="material-symbols-outlined">check_circle</span> Saved successfully!</div><% } %>

<div class="form-card">
    <h3 id="formTitle">Add Achievement</h3>
    <form action="/admin/achievements/save" method="POST">
        <input type="hidden" name="id" id="achId">
        <div class="form-grid">
            <div class="form-group">
                <label>Title (EN)</label>
                <input type="text" name="title_en" id="achTitleEn" required>
            </div>
            <div class="form-group">
                <label>Title (BN)</label>
                <input type="text" name="title_bn" id="achTitleBn" required>
            </div>
            <div class="form-group">
                <label>Sort Order</label>
                <input type="number" name="sort_order" id="achOrder" value="0">
            </div>
        </div>
        <div class="form-group">
            <label>Description (EN)</label>
            <div id="editor-en" style="height: 150px; background: white;"><%- '' %></div>
            <input type="hidden" name="description_en" id="description_en">
        </div>
        <div class="form-group">
            <label>Description (BN)</label>
            <div id="editor-bn" style="height: 150px; background: white;"><%- '' %></div>
            <input type="hidden" name="description_bn" id="description_bn">
        </div>
        <div class="form-actions">
            <button type="submit" class="btn btn-primary" id="saveBtn">Save Achievement</button>
            <button type="button" class="btn" onclick="resetForm()" style="display:none;" id="cancelBtn">Cancel</button>
        </div>
    </form>
</div>

<div class="action-bar">
    <h3 style="font-size:16px;color:#64748b;">Current Achievements</h3>
</div>

<table class="data-table">
    <thead>
        <tr>
            <th>Title (EN)</th>
            <th>Title (BN)</th>
            <th>Sort</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <% achievements.forEach(a => { %>
        <tr>
            <td><strong><%= a.title_en %></strong></td>
            <td><%= a.title_bn %></td>
            <td><%= a.sort_order %></td>
            <td>
                <button class="btn btn-sm btn-primary edit-btn" data-info='<%- JSON.stringify(a) %>'>Edit</button>
                <a href="/admin/achievements/delete/<%= a.id %>" class="btn btn-sm btn-danger" onclick="return confirm('Delete this achievement?')">Delete</a>
            </td>
        </tr>
        <% }) %>
    </tbody>
</table>

<script>
var quillEn = new Quill('#editor-en', { theme: 'snow', modules: { toolbar: [['bold', 'italic', 'underline'], [{ 'list': 'ordered'}, { 'list': 'bullet' }], ['clean']] } });
var quillBn = new Quill('#editor-bn', { theme: 'snow', modules: { toolbar: [['bold', 'italic', 'underline'], [{ 'list': 'ordered'}, { 'list': 'bullet' }], ['clean']] } });

document.querySelector('form').onsubmit = function() {
    document.getElementById('description_en').value = quillEn.root.innerHTML;
    document.getElementById('description_bn').value = quillBn.root.innerHTML;
};

document.querySelectorAll('.edit-btn').forEach(btn => {
    btn.addEventListener('click', function() {
        const a = JSON.parse(this.dataset.info);
        document.getElementById('formTitle').innerText = 'Edit Achievement';
        document.getElementById('achId').value = a.id;
        document.getElementById('achTitleEn').value = a.title_en;
        document.getElementById('achTitleBn').value = a.title_bn;
        document.getElementById('achOrder').value = a.sort_order;
        quillEn.root.innerHTML = a.description_en || '';
        quillBn.root.innerHTML = a.description_bn || '';
        document.getElementById('saveBtn').innerText = 'Update Achievement';
        document.getElementById('cancelBtn').style.display = 'inline-block';
        window.scrollTo({ top: 0, behavior: 'smooth' });
    });
});

function resetForm() {
    document.getElementById('formTitle').innerText = 'Add Achievement';
    document.getElementById('achId').value = '';
    document.getElementById('achTitleEn').value = '';
    document.getElementById('achTitleBn').value = '';
    document.getElementById('achOrder').value = '0';
    quillEn.root.innerHTML = '';
    quillBn.root.innerHTML = '';
    document.getElementById('saveBtn').innerText = 'Save Achievement';
    document.getElementById('cancelBtn').style.display = 'none';
}
</script>

<%- include('../partials/admin-footer') %>
